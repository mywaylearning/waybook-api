machine:
  node:
    version: 5.4.1

dependencies:
  pre:
    - bundle install

test:
  pre:
    - npm install

deployment:
  staging:
    branch: master
    commands:
      - bundle exec cap staging deploy
